Public Class frm_Etiqueta_Wineem_1

    Private Sub frm_Etiqueta_Wineem_1_Load(sender As System.Object, e As System.EventArgs) Handles MyBase.Load
        'MessageBox.Show("Has pulsado la FILA: " & e.RowIndex.ToString & " COLUMNA: " & e.ColumnIndex.ToString & " VALOR: ", dgv_Etiquetas.CurrentCell.Value.ToString)
        'LimpiarCeldas()
        'Dim row As DataGridViewRow = dgv_Etiquetas.CurrentRow
        'Try
        '    txtZona.Text = CStr(row.Cells("ZONA").Value)
        '    txtAgenteComercial.Text = CStr(row.Cells("AGENTE COMERCIAL").Value)
        '    txtLocalidad.Text = CStr(row.Cells("LOCALIDAD").Value)
        '    txtRuta.Text = CStr(row.Cells("RUTA").Value)
        '    txtCierre.Text = CStr(row.Cells("CIERRES").Value)
        '    txtCantidadCatalogos.Text = CStr(row.Cells("CANTIDAD DE CATALOGOS 09/10").Value)
        '    txtId.Text = CStr(row.Cells("ID").Value)
        'Catch ex As Exception
        'End Try

    End Sub
End Class